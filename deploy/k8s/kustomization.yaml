apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: lobsterroll
resources:
  - namespace.yaml
  - configmap.yaml
  - nats.yaml
  - leaderboard.yaml
  - enrichment.yaml
  - watcher.yaml
  - strategy.yaml
  - notifier.yaml
  - consensus.yaml
  - trader.yaml # ships at replicas: 0 (gated)
  # Secrets are applied separately from deploy/k8s/secrets/ (gitignored).
