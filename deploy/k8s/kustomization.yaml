apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: lobsterroll
resources:
  - namespace.yaml
  - configmap.yaml
  - nats.yaml
  - watcher.yaml
  # Secrets are applied separately from deploy/k8s/secrets/ (gitignored).
  # Remaining services are added here as each phase is implemented.
